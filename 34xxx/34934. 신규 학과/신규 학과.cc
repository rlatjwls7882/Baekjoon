#include<bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int n; cin >> n;
    while(n--) {
        string s; int a; cin >> s >> a;
        if(a==2026) {
            cout << s;
            break;
        }
    }
}