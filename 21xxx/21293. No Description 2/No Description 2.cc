#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int z, y, x, w, v, u, t, s, r, p, o, n, m, l, k, j, i, h, g, f, e, d, c, b, a; cin >> z >> y >> x >> w >> v >> u >> t >> s >> r >> p >> o >> n >> m >> l >> k >> j >> i >> h >> g >> f >> e >> d >> c >> b >> a;
    cout << a-b << '\n' << a*b << '\n' << a+b+1;
}